<Window
    x:Class="Aerospace.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Aerospace"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:Aerospace.Model"
    xmlns:viewModels="clr-namespace:Aerospace.ViewModels"
    Title="MainView"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance viewModels:MainViewModel}"
    mc:Ignorable="d">
    <DockPanel>
        <StackPanel
            HorizontalAlignment="Center"
            DockPanel.Dock="Bottom"
            Orientation="Horizontal">
            <Button x:Name="CreateJourneyAsync" HorizontalAlignment="Center">
                Create Journey
            </Button>
            <Button x:Name="LoadJourneyAsync" HorizontalAlignment="Center">
                Load Journey
            </Button>
            <Button x:Name="SaveJourneyAsync" HorizontalAlignment="Center">
                Save Journey
            </Button>
        </StackPanel>
        <ListView ItemsSource="{Binding ActiveJourneys}" SelectedItem="{Binding SelectedJourney, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type model:SpacecraftJourney}">
                    <TextBlock Text="{Binding Name, Mode=OneWay}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</Window>
